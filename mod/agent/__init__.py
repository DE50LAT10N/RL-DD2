# Legacy/mod-side Python agent package marker.
# Supports direct experiments against the plugin IPC environment.
# Most production live inference now goes through scripts/live_ppo.py.

"""Agent utilities for DD2 baseline runs."""

